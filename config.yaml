host: "localhost"
port: 5672
username: "guest"
password: "guest"
queues:
  loan: "payment.loan"
  investment: "payment.investment"
  resolved: "payment.resolved"
  unresolved: "payment.unresolved"
retry:
  max_attempts: 5
  delay_seconds: 10
